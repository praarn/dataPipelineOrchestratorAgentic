# Data Pipeline Orchestrator — dependencies
#
# Deliberately zero heavyweight framework dependencies (no FastAPI/Pydantic
# needed) so `pip install -r requirements.txt` is fast and has few points
# of failure. Tested against the versions below; anything reasonably recent
# should work.

Flask>=3.0
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1
python-docx>=1.1
reportlab>=4.0
